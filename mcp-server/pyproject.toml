[project]
name = "gofile-mcp"
version = "0.1.0"
description = "MCP stdio server wrapping the go-file /api/ai/* REST API"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
    "httpx>=0.27.0",
]

[project.scripts]
gofile-mcp = "gofile_mcp:main"
